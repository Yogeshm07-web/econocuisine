<%= form_with(model: @product, url: add_to_basket_path, method: :post, local: true) do |form| %>
  <%= form.hidden_field :product_id, value: @product.id %>
  <!-- Other form fields -->
  <%= form.submit "Add to Basket" %>
<% end %>
