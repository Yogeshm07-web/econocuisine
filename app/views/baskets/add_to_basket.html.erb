<%= form_with(model: @product, url: add_to_basket_path(@product.id), method: :post, local: true), data: {turbo_method: :post} do |form| %>
  <%= form.hidden_field :product_id, value: @product.id %>
  <!-- Other form fields -->
  <%= form.submit "Add to Basket" %>
<% end %>